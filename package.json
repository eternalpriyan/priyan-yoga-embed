{
  "name": "priyan-yoga-embed",
  "version": "0.1.0",
  "private": true,
  "description": "Custom Oclass content widgets for the priyan.yoga Squarespace site, served from Vercel serverless functions.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "test": "node --import tsx test/local.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
